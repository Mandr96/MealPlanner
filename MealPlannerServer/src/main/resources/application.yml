spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mealplanner_db
    username: postgres
    password: progettoDB
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQLDialect
